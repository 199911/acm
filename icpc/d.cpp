#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;

#define x first
#define y second
#define LL long long
#define FOR(i,a,b) for(int i = a; i < b; i++)
#define PII pair<int,int>

LL dist( PII a, PII b ) {
	return abs( a.x - b.x ) + abs( a.y - b.y );
}


int sy, n, m;
PII a[N];

int main() {
	return 0;
}
